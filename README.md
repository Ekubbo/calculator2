# Simple Calculator 🧮

A basic command-line calculator written in Python.

This project allows users to perform simple arithmetic operations — addition, subtraction, multiplication, and division — directly in the terminal.

## Features
- Interactive input via console
- Handles division by zero gracefully
- Clean and beginner-friendly code

## Usage
Run the program:
```bash
python calculator.py
